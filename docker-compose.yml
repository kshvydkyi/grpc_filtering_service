services:
  producer:
    build:
      context: .
      dockerfile: producer/Dockerfile
    ports:
      - "50051:50051"

  consumer:
    build:
      context: .
      dockerfile: consumer/Dockerfile
    depends_on:
      - producer
